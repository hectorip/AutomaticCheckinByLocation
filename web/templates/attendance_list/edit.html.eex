<h2>Edit attendance list</h2>

<%= render "form.html", changeset: @changeset,
                        action: attendance_list_path(@conn, :update, @attendance_list) %>

<%= link "Back", to: attendance_list_path(@conn, :index) %>
