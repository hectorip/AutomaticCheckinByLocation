<h2>New attendance list</h2>

<%= render "form.html", changeset: @changeset,
                        action: attendance_list_path(@conn, :create) %>

<%= link "Back", to: attendance_list_path(@conn, :index) %>
